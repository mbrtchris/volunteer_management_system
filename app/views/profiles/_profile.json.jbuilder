json.extract! profile, :id, :user_id, :is_educator, :is_volunteer, :personal_statement, :organization_id, :class_session_id, :first_name, :last_name, :created_at, :updated_at
json.url profile_url(profile, format: :json)