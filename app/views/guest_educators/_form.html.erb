<%= simple_form_for(@guest_educator) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email %>
    <%= f.input :name %>
    <%= f.association :class_session %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
