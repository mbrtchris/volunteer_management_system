<%= simple_form_for(@organization) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :type %>
    <%= f.association :class_session %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
