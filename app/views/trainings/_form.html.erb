<%= form_for @training, :html => { :class => 'form-horizontal form-model' } do |f| %>
  <div class="form-group">
    <%= f.label :training_date, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.datetime_select :training_date, :class => 'text-field form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :class_size, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.number_field :class_size, :class => 'text-field form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class='col-md-offset-2 col-md-10'>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                  trainings_path, :class => 'btn btn-default' %>
    </div>
  </div>
<% end %>
